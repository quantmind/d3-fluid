version: 2
jobs:
  tests:
    working_directory: ~/tests
      docker:
        - image: circleci/node
      steps:
        - checkout
        - run:
            name: install
            command: yarn install
        - run:
            name: test
            command: yarn test

workflows:
  version: 2
  build-deploy:
    jobs:
      - tests
